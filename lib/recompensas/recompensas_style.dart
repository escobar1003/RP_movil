// ─── recompensas_style.dart ─── RecompensasStyles

import 'package:flutter/material.dart';
import '../styles/colors.dart';

class RecompensasStyles {
  static const Color bg = SemanticColors.altBg;
  static const EdgeInsets headerPadding = EdgeInsets.fromLTRB(20, 20, 20, 24);
  static const double headerRadius = 28;
  static const EdgeInsets listPadding = EdgeInsets.fromLTRB(20, 16, 20, 20);
  static const EdgeInsets cardPadding = EdgeInsets.all(16);
  static const EdgeInsets cardMargin = EdgeInsets.only(bottom: 14);
  static const double cardRadius = 20;
  static const double storeIconSize = 60;
  static const double storeIconRadius = 16;
  static const double bannerRadius = 16;
  static const EdgeInsets bannerPadding = EdgeInsets.symmetric(horizontal: 18, vertical: 14);
  static const double bannerIconSize = 44;
  static const double bannerIconRadius = 12;
  static const EdgeInsets badgePadding = EdgeInsets.symmetric(horizontal: 10, vertical: 4);
  static const double badgeRadius = 20;
}